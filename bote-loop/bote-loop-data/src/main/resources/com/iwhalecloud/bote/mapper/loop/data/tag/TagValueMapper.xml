<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iwhalecloud.bote.mapper.loop.data.tag.TagValueMapper">
  <!-- 结果映射 -->
  <resultMap id="BaseResultMap" type="com.iwhalecloud.bote.entity.loop.data.tag.TagValueEntity">
    <id column="id" property="id" jdbcType="BIGINT"/>
    <result column="app_id" property="appId" jdbcType="INTEGER"/>
    <result column="space_id" property="spaceId" jdbcType="BIGINT"/>
    <result column="version_num" property="versionNum" jdbcType="INTEGER"/>
    <result column="tag_key_id" property="tagKeyId" jdbcType="BIGINT"/>
    <result column="tag_value_id" property="tagValueId" jdbcType="BIGINT"/>
    <result column="tag_value_name" property="tagValueName" jdbcType="VARCHAR"/>
    <result column="description" property="description" jdbcType="VARCHAR"/>
    <result column="status" property="status" jdbcType="VARCHAR"/>
    <result column="parent_value_id" property="parentValueId" jdbcType="BIGINT"/>
    <result column="created_by" property="createdBy" jdbcType="VARCHAR"/>
    <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    <result column="updated_by" property="updatedBy" jdbcType="VARCHAR"/>
    <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
  </resultMap>

  <!-- 基础字段 -->
  <sql id="Base_Column_List">
    id
    , app_id, space_id, version_num, tag_key_id, tag_value_id, tag_value_name,
    description, status, parent_value_id, created_by, created_at, updated_by, updated_at
  </sql>

  <!-- 根据spaceId和id查询 -->
  <select id="selectBySpaceIdAndId" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM tag_value
    WHERE space_id = #{spaceId} AND id = #{id}
  </select>

  <!-- 根据条件查询 -->
  <select id="selectByCondition" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM tag_value
    <where>
      <if test="spaceId != null">
        AND space_id = #{spaceId}
      </if>
      <if test="ids != null and ids.size() > 0">
        AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
          #{id}
        </foreach>
      </if>
      <if test="status != null">
        AND status = #{status}
      </if>
      <if test="tagKeyId != null">
        AND tag_key_id = #{tagKeyId}
      </if>
      <if test="version != null">
        AND version_num = #{version}
      </if>
      <if test="tagValueIds != null and tagValueIds.size() > 0">
        AND tag_value_id IN
        <foreach collection="tagValueIds" item="tagValueId" open="(" separator="," close=")">
          #{tagValueId}
        </foreach>
      </if>
    </where>
    ORDER BY id DESC
  </select>

  <!-- 根据条件统计数量 -->
  <select id="countByCondition" resultType="java.lang.Long">
    SELECT COUNT(1)
    FROM tag_value
    <where>
      <if test="spaceId != null">
        AND space_id = #{spaceId}
      </if>
      <if test="ids != null and ids.size() > 0">
        AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
          #{id}
        </foreach>
      </if>
      <if test="status != null">
        AND status = #{status}
      </if>
      <if test="tagKeyId != null">
        AND tag_key_id = #{tagKeyId}
      </if>
      <if test="version != null">
        AND version_num = #{version}
      </if>
      <if test="tagValueIds != null and tagValueIds.size() > 0">
        AND tag_value_id IN
        <foreach collection="tagValueIds" item="tagValueId" open="(" separator="," close=")">
          #{tagValueId}
        </foreach>
      </if>
    </where>
  </select>

  <!-- 批量插入 -->
  <insert id="batchInsert" parameterType="java.util.List">
    INSERT INTO tag_value (
    id, app_id, space_id, version_num, tag_key_id, tag_value_id, tag_value_name,
    description, status, parent_value_id, created_by, created_at, updated_by, updated_at
    ) VALUES
    <foreach collection="list" item="item" separator=",">
      (
      #{item.id}, #{item.appId}, #{item.spaceId}, #{item.versionNum},
      #{item.tagKeyId}, #{item.tagValueId}, #{item.tagValueName}, #{item.description},
      #{item.status}, #{item.parentValueId}, #{item.createdBy}, #{item.createdAt},
      #{item.updatedBy}, #{item.updatedAt}
      )
    </foreach>
  </insert>

  <!-- 根据条件更新 - 使用PO层对象，所有判断在XML中处理 -->
  <update id="updateByCondition">
    UPDATE tag_value
    <set>
      <if test="updateEntity.tagValueName != null and updateEntity.tagValueName != ''">
        tag_value_name = #{updateEntity.tagValueName},
      </if>
      <if test="updateEntity.description != null">
        description = #{updateEntity.description},
      </if>
      <if test="updateEntity.status != null and updateEntity.status != ''">
        status = #{updateEntity.status},
      </if>
      <if test="updateEntity.updatedBy != null and updateEntity.updatedBy != ''">
        updated_by = #{updateEntity.updatedBy},
      </if>
      <if test="updateEntity.updatedAt != null">
        updated_at = #{updateEntity.updatedAt},
      </if>
      <if test="updateEntity.versionNum != null">
        version_num = #{updateEntity.versionNum},
      </if>
      <if test="updateEntity.tagKeyId != null">
        tag_key_id = #{updateEntity.tagKeyId},
      </if>
      <if test="updateEntity.tagValueId != null">
        tag_value_id = #{updateEntity.tagValueId},
      </if>
      <if test="updateEntity.parentValueId != null">
        parent_value_id = #{updateEntity.parentValueId},
      </if>
    </set>
    WHERE space_id = #{spaceId} AND id = #{id}
  </update>

  <!-- 更新状态 -->
  <update id="updateStatusByCondition">
    UPDATE tag_value
    <set>
      status = #{status}
      <if test="updatedAt != null and updatedAt != ''">
        , updated_at = #{updatedAt}
      </if>
      <if test="updatedBy != null and updatedBy != ''">
        , updated_by = #{updatedBy}
      </if>
    </set>
    WHERE space_id = #{spaceId} AND tag_key_id = #{tagKeyId} AND version_num = #{versionNum}
  </update>

  <!-- 根据spaceId和id删除 -->
  <delete id="deleteBySpaceIdAndId">
    DELETE
    FROM tag_value
    WHERE space_id = #{spaceId}
      AND id = #{id}
  </delete>
</mapper>
