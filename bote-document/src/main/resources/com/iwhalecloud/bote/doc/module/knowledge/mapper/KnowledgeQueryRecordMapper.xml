<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iwhalecloud.bote.doc.module.knowledge.mapper.KnowledgeQueryRecordMapper">
  <!-- @foramtter:off -->
  <insert id="insertQueryRecord">
    INSERT INTO bt_knowledge_query_record (id, knowledge_id, query_source, content, tenant_id, creator_id, created_time)
    VALUES (
      #{dto.id},
      #{dto.knowledgeId},
      #{dto.querySource},
      #{dto.content},
      #{dto.tenantId},
      #{dto.creatorId},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>
    )
  </insert>

  <select id="countQueryRecords" resultType="java.lang.Integer">
    SELECT COUNT(*)
    FROM bt_knowledge_query_record
    WHERE knowledge_id = #{knowledgeId}
      AND query_source = #{querySource}
      AND tenant_id = #{tenantId}
  </select>

  <select id="selectQueryContentList" resultType="java.lang.String">
    SELECT content
    FROM bt_knowledge_query_record
    WHERE knowledge_id = #{knowledgeId}
      AND query_source = #{querySource}
      AND tenant_id = #{tenantId}
    ORDER BY created_time DESC
  </select>
</mapper>
