<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iwhalecloud.bote.doc.module.knowledge.mapper.BtDcQaRecordKbRelManageMapper">

  <sql id="btDcQaRecordKbRelColumns">
    a.rel_id,
    a.qa_id,
    a.kb_id,
    a.tenant_id,
    a.creator_id,
    a.created_time,
    a.updated_time,
    a.status_cd
  </sql>

  <sql id="batchQueryCondition">
    AND a.status_cd = '00A'
    <if test="query.searchContent != null and query.searchContent != ''">
      AND (a.code LIKE
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.like-left"/>
      #{query.searchContent}
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.like-right"/>
      OR a.name LIKE
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.like-left"/>
      #{query.searchContent}
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.like-right"/>
      )
    </if>
  </sql>

  <insert id="insertBtDcQaRecordKbRel">
    INSERT INTO bt_dc_qa_record_kb_rel (
    rel_id,
    qa_id,
    kb_id,
    tenant_id,
    creator_id,
    created_time,
    updated_time,
    status_cd
    ) VALUES (
    #{dto.relId},
    #{dto.qaId},
    #{dto.kbId},
    #{dto.tenantId},
    #{dto.creatorId},
    <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
    <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
    #{dto.statusCd}
    )
  </insert>

  <insert id="batchInsertBtDcQaRecordKbRel">
    INSERT INTO bt_dc_qa_record_kb_rel (
    rel_id,
    qa_id,
    kb_id,
    tenant_id,
    creator_id,
    created_time,
    updated_time,
    status_cd
    ) VALUES
    <foreach collection="list" item="dto" index="index" separator=", ">
      (
      #{dto.relId},
      #{dto.qaId},
      #{dto.kbId},
      #{dto.tenantId},
      #{dto.creatorId},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      #{dto.statusCd}
      )
    </foreach>
  </insert>

  <insert id="batchInsertBtDcQaRecordKbRel" databaseId="oracle">
    INSERT ALL
    <foreach collection="list" item="dto" index="index" separator=" ">
      INTO bt_dc_qa_record_kb_rel (
      rel_id,
      qa_id,
      kb_id,
      tenant_id,
      creator_id,
      created_time,
      updated_time,
      status_cd
      ) VALUES (
      #{dto.relId},
      #{dto.qaId},
      #{dto.kbId},
      #{dto.tenantId},
      #{dto.creatorId},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      #{dto.statusCd}
      )
    </foreach>
    SELECT 1 FROM dual
  </insert>

  <update id="deleteBtDcQaRecordKbRel">
    update bt_dc_qa_record_kb_rel set status_cd='00X',updated_time =<include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/> where qa_id=#{qaId} and tenant_id = #{tenantId}
  </update>

  <update id="deleteBtDcQaRecordKbRelByKnowledgeId">
    update bt_dc_qa_record_kb_rel 
    set status_cd='00X',updated_time =<include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/> 
    where qa_id=#{qaId} and kb_id=#{knowledgeId} and tenant_id = #{tenantId}
  </update>

</mapper>
