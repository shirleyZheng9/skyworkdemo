<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iwhalecloud.bote.mapper.model.LargeModelQueryMapper">
  <select id="selectLargeModelList" resultType="java.lang.Long">
   select a.model_id from bt_library_large_model a where (a.tenant_id = #{tenantId} OR (a.tenant_id = -1 AND a.is_public = 'T')) and a.status_cd = '00A' ORDER BY a.updated_time DESC
  </select>
</mapper>
