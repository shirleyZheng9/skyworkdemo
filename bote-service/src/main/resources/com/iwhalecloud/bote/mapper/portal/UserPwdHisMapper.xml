<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iwhalecloud.bote.mapper.portal.UserPwdHisMapper">

  <sql id="columns">
    a.his_id,
    a.user_id,
    a.password,
    a.status_cd,
    a.creator_id,
    a.created_time,
    a.updator_id,
    a.updated_time,
    a.remark
  </sql>

  <insert id="insertUserPwdHis">
    INSERT INTO bt_user_pwd_his (
      his_id,
      user_id,
      password,
      status_cd,
      creator_id,
      created_time,
      updator_id,
      updated_time,
      remark
    ) VALUES (
      #{dto.hisId},
      #{dto.userId},
      #{dto.password},
      #{dto.statusCd},
      #{dto.creatorId},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      #{dto.updatorId},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      #{dto.remark}
    )
  </insert>

  <select id="selectPageByUserId" resultType="com.iwhalecloud.bote.dto.portal.UserPwdHisDTO">
    SELECT his_id, user_id, password
    FROM bt_user_pwd_his
    WHERE status_cd = '00A'
      AND user_id = #{userId}
    ORDER BY created_time DESC
  </select>

</mapper>
