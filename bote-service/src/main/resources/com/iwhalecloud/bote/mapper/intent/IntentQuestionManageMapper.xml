<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iwhalecloud.bote.mapper.intent.IntentQuestionManageMapper">

  <sql id="intentQuestionColumns">
    a.question_id,
    a.scene_id,
    a.tenant_id,
    a.question,
    a.attribute,
    a.created_time,
    a.status_cd,
    a.updated_time,
    a.creator_id,
    a.updator_id,
    a.remark
  </sql>

  <sql id="batchQueryCondition">
      AND a.status_cd = '00A'
    <if test="query.tenantId != null">
      AND a.tenant_id = #{query.tenantId}
    </if>
    <if test="query.sceneId != null">
      AND a.scene_id = #{query.sceneId}
    </if>
    <if test="query.sceneIds != null and !query.sceneIds.isEmpty()">
      AND a.scene_id IN (<foreach collection="query.sceneIds" item="sceneId" separator=",">#{sceneId}</foreach>)
    </if>
  </sql>

  <select id="existsQuestion" resultType="boolean">
    SELECT CASE WHEN count(*) &gt; 0 THEN 1 ELSE 0 END AS existence
    FROM bt_intention_question
    WHERE status_cd = '00A'
      AND question = #{dto.question}
      AND tenant_id = #{dto.tenantId}
    <if test="dto.sceneId != null">
      AND scene_id = #{dto.sceneId}
    </if>
    <if test="dto.questionId != null">
      AND question_id != #{dto.questionId}
    </if>
  </select>

  <select id="existsQuestion" resultType="boolean" databaseId="oracle">
    SELECT CASE WHEN count(*) &gt; 0 THEN 1 ELSE 0 END AS existence
    FROM bt_intention_question
    WHERE status_cd = '00A'
    AND to_char(question) = #{dto.question}
    AND tenant_id = #{dto.tenantId}
    <if test="dto.sceneId != null">
      AND scene_id = #{dto.sceneId}
    </if>
    <if test="dto.questionId != null">
      AND question_id != #{dto.questionId}
    </if>
  </select>

  <select id="getIntentQuestion" resultType="com.iwhalecloud.bote.dto.intent.IntentQuestionDTO">
    SELECT
    <include refid="intentQuestionColumns"/>
    FROM bt_intention_question a
    WHERE question_id = #{id}
    AND status_cd = '00A'
    AND tenant_id = #{tenantId}
  </select>

  <insert id="insertIntentQuestion">
    INSERT INTO bt_intention_question (
    question_id,
    scene_id,
    tenant_id,
    question,
    attribute,
    created_time,
    status_cd,
    updated_time,
    creator_id,
    updator_id,
    remark
    ) VALUES (
    #{dto.questionId},
    #{dto.sceneId},
    #{dto.tenantId},
    #{dto.question},
    #{dto.attribute},
    <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
    #{dto.statusCd},
    <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
    #{dto.creatorId},
    #{dto.updatorId},
    #{dto.remark}
    )
  </insert>

  <insert id="batchInsertIntentQuestion">
    INSERT INTO bt_intention_question (
    question_id,
    scene_id,
    tenant_id,
    question,
    attribute,
    created_time,
    status_cd,
    updated_time,
    creator_id,
    updator_id,
    remark
    ) VALUES
    <foreach collection="list" item="dto" index="index" separator=", ">
      (
      #{dto.questionId},
      #{dto.sceneId},
      #{dto.tenantId},
      #{dto.question},
      #{dto.attribute},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      #{dto.statusCd},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      #{dto.creatorId},
      #{dto.updatorId},
      #{dto.remark}
      )
    </foreach>
  </insert>

  <insert id="batchInsertIntentQuestion" databaseId="oracle">
    INSERT ALL
    <foreach collection="list" item="dto" index="index" separator=" ">
      INTO bt_intention_question (
      question_id,
      scene_id,
      tenant_id,
      question,
      attribute,
      created_time,
      status_cd,
      updated_time,
      creator_id,
      updator_id,
      remark
      ) VALUES (
      #{dto.questionId},
      #{dto.sceneId},
      #{dto.tenantId},
      #{dto.question},
      #{dto.attribute},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      #{dto.statusCd},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      #{dto.creatorId},
      #{dto.updatorId},
      #{dto.remark}
      )
    </foreach>
    SELECT 1 FROM dual
  </insert>

  <update id="updateIntentQuestion">
    UPDATE bt_intention_question
    <set>
      <if test="dto.sceneId != null">
        scene_id = #{dto.sceneId},
      </if>
      <if test="dto.question != null and dto.question != ''">
        question = #{dto.question},
      </if>
      <if test="dto.statusCd != null and dto.statusCd != ''">
        status_cd = #{dto.statusCd},
      </if>
      <if test="dto.remark != null and dto.remark != ''">
        remark = #{dto.remark},
      </if>
      attribute = #{dto.attribute},
      updated_time = <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      updator_id = #{dto.updatorId},
    </set>
    WHERE question_id = #{dto.questionId}
      AND tenant_id = #{dto.tenantId}
  </update>

  <update id="deleteIntentQuestion">
    UPDATE bt_intention_question
    SET status_cd = '00X',
    updator_id = #{updatorId},
    updated_time = <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>
    WHERE question_id = #{id}
    AND tenant_id = #{tenantId}
  </update>

  <select id="selectIntentQuestionList" resultType="com.iwhalecloud.bote.dto.intent.IntentQuestionDTO">
    SELECT
    <include refid="intentQuestionColumns"/>
    FROM bt_intention_question a
    WHERE 1=1
    <include refid="batchQueryCondition"/>
  </select>

  <select id="selectIntentQuestionPage" resultType="com.iwhalecloud.bote.dto.intent.IntentQuestionDTO">
    SELECT
    <include refid="intentQuestionColumns"/>,
    u.real_name AS "creatorName", s.scene_name AS "sceneName"
    FROM bt_intention_question a
    LEFT JOIN bt_user u ON u.user_id = a.creator_id
    LEFT JOIN bt_bot_scene s ON s.scene_id = a.scene_id AND s.tenant_id = a.tenant_id
    WHERE 1=1
    <include refid="batchQueryCondition"/>
    <if test="query.searchContent != null and query.searchContent != ''">
      AND a.question LIKE
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.like-left"/>
      #{query.searchContent}
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.like-right"/>
    </if>
    ORDER BY a.updated_time DESC
  </select>

  <select id="selectQuestionsByTenantId" resultType="com.iwhalecloud.bote.dto.intent.IntentQuestionDTO">
    SELECT a.tenant_id, a.scene_id, b.scene_name,
           a.question_id, a.question, a.attribute
    FROM bt_intention_question a
           LEFT JOIN bt_bot_scene b ON b.scene_id = a.scene_id AND b.tenant_id = a.tenant_id AND b.status_cd = '00A'
    WHERE a.status_cd = '00A'
      AND b.scene_status = '1'
      AND a.tenant_id = #{tenantId}
  </select>

  <select id="selectSceneAndQuestionsByIds" resultType="com.iwhalecloud.bote.dto.intent.IntentionMatchItemWithSceneDTO">
    SELECT a.question_id, a.scene_id, d.scene_name, a.question, a.attribute
    FROM bt_intention_question a
    LEFT JOIN bt_bot_scene d ON d.scene_id = a.scene_id AND d.tenant_id = a.tenant_id AND d.status_cd = '00A'
    WHERE a.status_cd = '00A'
    AND a.question_id IN (<foreach collection="ids" item="id" separator=",">#{id}</foreach>)
    AND a.tenant_id = #{tenantId}
  </select>

  <select id="selectSceneIdByQuestionId" resultType="java.lang.Long">
    SELECT scene_id
    FROM bt_intention_question
    WHERE status_cd = '00A'
      AND question_id = #{questionId}
      AND tenant_id = #{tenantId}
  </select>

  <update id="deleteIntentQuestionByQuestion">
    UPDATE bt_intention_question
    SET status_cd = '00X',
    updator_id = #{updatorId},
    updated_time = <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>
    WHERE question = #{question}
    AND tenant_id = #{tenantId}
  </update>
</mapper>
