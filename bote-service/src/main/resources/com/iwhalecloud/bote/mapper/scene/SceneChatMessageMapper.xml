<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iwhalecloud.bote.mapper.scene.SceneChatMessageMapper">
  <!-- @formatter:off -->
  <sql id="chatMessageColumns">
    message_id,
    scene_id,
    conversation_id,
    context_id,
    seq,
    message_role,
    message_content,
    tool_call_id,
    tool_name,
    tool_arguments,
    user_id,
    create_time
  </sql>

  <sql id="simpleChatMessageColumns">
    seq,
    message_role,
    message_content,
    tool_call_id,
    tool_name,
    tool_arguments
  </sql>

  <insert id="batchInsertMessage">
    INSERT INTO bt_chat_message (<include refid="chatMessageColumns"/>)
    VALUES
    <foreach collection="messages" item="message" separator=", ">
      (
      #{message.messageId},
      #{message.sceneId},
      #{message.conversationId},
      #{message.contextId},
      #{message.seq},
      #{message.messageRole},
      #{message.messageContent},
      #{message.toolCallId},
      #{message.toolName},
      #{message.toolArguments},
      #{message.userId},
      #{message.createTime})
    </foreach>
  </insert>

  <insert id="batchInsertMessage" databaseId="oracle">
    INSERT ALL
    <foreach collection="messages" item="message" separator=" ">
    INTO bt_chat_message (<include refid="chatMessageColumns"/>)
    VALUES (
      #{message.messageId},
      #{message.sceneId},
      #{message.conversationId},
      #{message.contextId},
      #{message.seq},
      #{message.messageRole},
      #{message.messageContent},
      #{message.toolCallId},
      #{message.toolName},
      #{message.toolArguments},
      #{message.userId},
      #{message.createTime})
    </foreach>
    SELECT 1 FROM dual
  </insert>

  <select id="selectSimpleMessagesByContextId" resultType="com.iwhalecloud.bote.dto.scene.SceneChatMessageDTO">
    SELECT <include refid="simpleChatMessageColumns"/>
    FROM bt_chat_message
    WHERE context_id = #{contextId}
    ORDER BY seq
  </select>

  <insert id="insertProcess">
    INSERT INTO bt_flow_scene_process (
    id,
    tenant_id,
    scene_id,
    context_id,
    main_scene_id,
    main_context_id,
    main_conversation_id,
    node_code,
    flow_status,
    created_time,
    status_cd,
    updated_time,
    creator_id,
    updator_id,
    remark
    ) VALUES (
    #{dto.id},
    #{dto.tenantId},
    #{dto.sceneId},
    #{dto.contextId},
    #{dto.mainSceneId},
    #{dto.mainContextId},
    #{dto.mainConversationId},
    #{dto.nodeCode},
    #{dto.flowStatus},
    <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
    #{dto.statusCd},
    <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
    #{dto.creatorId},
    #{dto.updatorId},
    #{dto.remark}
    )
  </insert>

  <select id="getProcess" resultType="com.iwhalecloud.bote.dto.scene.FlowSceneProcessDTO">
    SELECT
    tenant_id,
    scene_id,
    context_id,
    node_code,
    flow_status,
    main_scene_id,
    main_context_id,
    main_conversation_id
    FROM bt_flow_scene_process
    WHERE status_cd = '00A'
      AND context_id = #{contextId}
  </select>

  <update id="updateProcess">
    UPDATE bt_flow_scene_process
    SET flow_status = #{flowStatus},
    updated_time = <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>
    WHERE context_id = #{contextId}
      AND scene_id = #{sceneId}
  </update>
</mapper>
