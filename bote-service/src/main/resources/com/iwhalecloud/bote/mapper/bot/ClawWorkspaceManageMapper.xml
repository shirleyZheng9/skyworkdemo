<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iwhalecloud.bote.mapper.bot.ClawWorkspaceManageMapper">

  <sql id="workspaceColumns">
    a.id,
    a.tenant_id,
    a.scene_id,
    a.file_name,
    a.file_content,
    a.memory_type,
    a.created_time,
    a.status_cd,
    a.updated_time,
    a.creator_id,
    a.updator_id,
    a.remark
  </sql>

  <insert id="insertClawWorkspace">
    INSERT INTO bt_claw_workspace (
      id, tenant_id, scene_id, file_name, file_content, memory_type,
      created_time, status_cd, updated_time, creator_id, updator_id, remark
    ) VALUES (
      #{dto.id}, #{dto.tenantId}, #{dto.sceneId}, #{dto.fileName}, #{dto.fileContent}, #{dto.memoryType},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>, #{dto.statusCd},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>, #{dto.creatorId}, #{dto.updatorId}, #{dto.remark}
    )
  </insert>

  <insert id="batchInsertClawWorkspace">
    INSERT INTO bt_claw_workspace (
      id, tenant_id, scene_id, file_name, file_content, memory_type,
      created_time, status_cd, updated_time, creator_id, updator_id, remark
    )
    VALUES
    <foreach collection="list" item="dto" separator=",">
      (
        #{dto.id}, #{dto.tenantId}, #{dto.sceneId}, #{dto.fileName}, #{dto.fileContent}, #{dto.memoryType},
        <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>, #{dto.statusCd},
        <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>, #{dto.creatorId}, #{dto.updatorId}, #{dto.remark}
      )
    </foreach>
  </insert>

  <insert id="batchInsertClawWorkspace" databaseId="oracle">
    INSERT ALL
    <foreach collection="list" item="dto" separator=" ">
      INTO bt_claw_workspace (
        id, tenant_id, scene_id, file_name, file_content, memory_type,
        created_time, status_cd, updated_time, creator_id, updator_id, remark
      ) VALUES (
        #{dto.id}, #{dto.tenantId}, #{dto.sceneId}, #{dto.fileName}, #{dto.fileContent}, #{dto.memoryType},
        <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>, #{dto.statusCd},
        <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>, #{dto.creatorId}, #{dto.updatorId}, #{dto.remark}
      )
    </foreach>
    SELECT 1 FROM dual
  </insert>

  <update id="updateClawWorkspace">
    UPDATE bt_claw_workspace
    <set>
      <if test="dto.checkFieldUpdateFlag('fileName')">
        file_name = #{dto.fileName},
      </if>
      <if test="dto.checkFieldUpdateFlag('fileContent')">
        file_content = #{dto.fileContent},
      </if>
      <if test="dto.checkFieldUpdateFlag('memoryType')">
        memory_type = #{dto.memoryType},
      </if>
      <if test="dto.checkFieldUpdateFlag('statusCd')">
        status_cd = #{dto.statusCd},
      </if>
      <if test="dto.checkFieldUpdateFlag('remark')">
        remark = #{dto.remark},
      </if>
      updated_time = <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      updator_id = #{dto.updatorId}
    </set>
    WHERE id = #{dto.id}
      AND tenant_id = #{dto.tenantId}
  </update>

  <select id="selectClawWorkspaceList" resultType="com.iwhalecloud.bote.dto.bot.ClawWorkspaceDTO">
    SELECT
    <include refid="workspaceColumns"/>
    FROM bt_claw_workspace a
    WHERE status_cd = '00A'
      AND tenant_id = #{tenantId}
      AND scene_id = #{sceneId}
      AND file_name IN (<foreach collection="fileNames" item="name" separator=",">#{name}</foreach>)
    ORDER BY a.created_time
  </select>

</mapper>
