<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iwhalecloud.bote.mapper.agent.SessionStateMapper">
  <!-- @formatter:off -->
  <insert id="insert">
    INSERT INTO bt_session_state (
      id,
      session_id,
      state_scope,
      state_data,
      created_time,
      updated_time
    ) VALUES (
      #{entity.id},
      #{entity.sessionId},
      #{entity.stateScope},
      #{entity.stateData},
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>,
      <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>
    )
  </insert>

  <update id="update">
    UPDATE bt_session_state
    SET state_data = #{entity.stateData},
        updated_time = <include refid="com.iwhalecloud.bss.litchi.mybatis.mapper.SqlFragments.func_now"/>
    WHERE id = #{entity.id}
  </update>

  <select id="selectBySessionId" resultType="com.iwhalecloud.bote.entity.agent.SessionStateEntity">
    SELECT id, state_scope, state_data
    FROM bt_session_state
    WHERE session_id = #{sessionId}
      AND state_data IS NOT NULL
  </select>

  <select id="selectBySessionIdAndScope" resultType="com.iwhalecloud.bote.entity.agent.SessionStateEntity">
    SELECT id, state_data
    FROM bt_session_state
    WHERE session_id = #{sessionId}
      AND state_scope = #{scope}
  </select>

  <delete id="deleteBySessionIdAndScope">
    DELETE
    FROM bt_session_state
    WHERE session_id = #{sessionId}
      AND state_scope = #{scope}
  </delete>

  <delete id="deleteBySessionIds">
    DELETE
    FROM bt_session_state
    WHERE session_id IN <foreach collection="sessionIds" item="sessionId" open="(" separator="," close=")">#{sessionId}</foreach>
  </delete>
</mapper>
